/* fuzzyTimeJS main */

// Base function.
var fuzzyTimeJS = function() {
  // Add functionality here.
  return true;
};


// Version.
fuzzyTimeJS.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.fuzzyTimeJS = fuzzyTimeJS;
